import ReactDOM from "react-dom/client";
import CafesTable from "./CafesTable.jsx";

const dom = <CafesTable />;

const root = ReactDOM.createRoot(document.getElementById("container"));
root.render(dom);
